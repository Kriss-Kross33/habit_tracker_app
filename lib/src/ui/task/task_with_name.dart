import 'package:flutter/material.dart';

class TaskWithName extends StatelessWidget {
  const TaskWithName({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
